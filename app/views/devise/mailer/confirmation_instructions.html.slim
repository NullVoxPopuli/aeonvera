= content_for :header do
  | Confirm Your Email
= content_for :body do
  p = "Welcome #{@resource.name}!"
  p You can confirm your account email through the link below:
  - domain = APPLICATION_CONFIG['domain'][Rails.env]
  - protocol = Rails.env.development? ? 'http://' : 'https://'
  - path = '/confirmation?confirmation_token=' + @resource.confirmation_token
  - url = protocol + domain + path
  p = link_to 'Confirm my account', url
  p 
    | If you need to, you can review the&nbsp;
    - tos_url = protocol + domain + '/welcome/tos'
    = link_to 'Terms of Service', tos_url
  br
  p
    | If you registered for an event at the door and have not set your password, you will have to reset your password after confirming your email before you may login.
  br
  
= render file: "/layouts/email"
