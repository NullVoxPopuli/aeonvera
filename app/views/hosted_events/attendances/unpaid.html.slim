= link_to "Download CSV", unpaid_hosted_event_attendances_path(@event, format: "csv")
= render partial: "/hosted_events/attendances/attendances", locals: { attendances: @attendances,  title: "#{@attendances.count} Unpaid Attendees for #{@event.name}", show_owed: true}