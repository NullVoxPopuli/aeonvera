= link_to "Download CSV", volunteers_hosted_event_path(@event, format: "csv")

= render partial: "/hosted_events/attendances/attendances", locals: { attendances: @event.attendances.volunteers, title: "Volunteers" }